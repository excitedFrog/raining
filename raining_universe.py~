# Python 3.6.1

import numpy as np

from universe import Universe

class RainingUniverse(Universe):
    def __init__(self):
        super().__init__()
        self.rain_intensity = int()  # raindrops per second
        self.rain_speed = 900  # 9m/s
        
        self._human_size = tuple()  # Dimensionality consistent to that of universe.size
        self.human_space = None
        self.human_speed = 800

    def rain_initialize(self):
        initial_density = self.size[-1] % self.rain_speed * self.rain_intensity
        `


